apiVersion: v1
kind: ConfigMap
metadata:
  name: observer-dashboard
  namespace: monitoring
  labels:
    grafana_dashboard: "1"
data:
  observer-dashboard.json: |
    {}  # <-파일로 생성해서 넣을 거라 비워도 됨(kustomize가 덮어씀)
