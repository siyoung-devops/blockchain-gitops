global:
  scrape_interval: 10s  

rule_files:
  - "/etc/prometheus/alerts.yml"

scrape_configs:
  - job_name: "bitcoin-observer"
    static_configs:
      - targets:
          - "observer:9100"

alerting:
  alertmanagers:
    - static_configs:
        - targets:
            - "alertmanager:9093"