apiVersion: v1
kind: Service
metadata:
  name: observer
  namespace: monitoring
  labels:
    app: observer
spec:
  type: ClusterIP
  selector:
    app: observer
  ports:
    - name: metrics          
      port: 9100
      targetPort: 9100